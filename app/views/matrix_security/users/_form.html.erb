<%= form_for(@matrix_security_user) do |f| %>
  <% if @matrix_security_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@matrix_security_user.errors.count, "error") %> prohibited this matrix_security_user from being saved:</h2>

      <ul>
      <% @matrix_security_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
